[build]
  command = "npm run build"
  publish = ".generated-public"

[build.environment]
  NODE_VERSION = "22"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"
  external_node_modules = ["jsdom"]
  included_files = ["index.html", "assets/js/**", "scripts/**"]
